export const add = (left, right) => left + right
export const subtract = (left, right) => left - right
export const multiply = (left, right) => left * right
export const square = (number) => multiply(number, number)
